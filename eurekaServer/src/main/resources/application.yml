spring:
  application:
    name: eurekaserver
  security:
    basic:
      enable: true
    user:
      name: guest
      password: guest

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
